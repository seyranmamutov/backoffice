spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: root
    password:
  batch:
    initialize-schema: always
  jpa:
    show-sql: true
    open-in-view: false

backoffice:
  brands-watch-directory: /tmp/backoffice/brands
  inventories-watch-directory: /tmp/backoffice/inventories